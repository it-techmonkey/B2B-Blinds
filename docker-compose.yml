services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: b2b_oms
    ports:
      - "5432:5432"
    volumes:
      - b2b_oms_pgdata:/var/lib/postgresql/data

volumes:
  b2b_oms_pgdata:
